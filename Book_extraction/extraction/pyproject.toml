[project]
name = "dental-textbook-extraction"
version = "0.1.0"
description = "Resumable, auditable Docling extraction for dental textbooks"
requires-python = ">=3.12,<3.13"
dependencies = ["docling>=2.100,<3"]

[tool.uv]
package = false
